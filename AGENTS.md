# “别骂了”项目指南

## 项目目的

本仓库包含“别骂了”微信小程序的产品定义、应用代码、AI 角色引擎、评估和交付资产。

产品通过可配置的“暴君”角色，为用户提供基于证据、极其直接的专业反馈。权威产品定义位于 `docs/product-framework.md`。

## 协作模式

用户是产品负责人，可能没有编程背景。

- 用户负责产品目标、用户体验、优先级、品味和范围决策。
- Codex 负责技术选择、实现、验证、文档更新和 Git 整洁性。
- 只有当决策会实质改变产品行为、范围、成本、隐私或发布风险时，才询问用户。
- 对实现细节优先作出合理、可逆的选择；用户说“直接做”时不要增加文档关卡，用户说“先讨论”时只比较有实质差异的方案并给出推荐。

## 任务路由

- 产品范围、用户流程、角色系统、表达边界、质量分层和开发阶段：阅读 `docs/product-framework.md`。
- 已经确定的产品或架构取舍：先查 `docs/decisions/README.md` 和对应 ADR；新增长期决策时写入 `docs/decisions/`。
- 生产评审接口、报告结构、文件协议和环境变量：阅读 `docs/api/review-api.md`。
- CloudBase 部署、真机验收和运行故障：阅读 `docs/cloudbase-deployment.md`。
- 修改小程序、云函数或 AI 核心时，分别从 `apps/miniprogram/`、`services/cloud-function/`、`packages/ai-core/` 进入；`services/api/` 仅是本地测试 Adapter。

## 必须遵守的产品护栏

- 专业诊断是产品核心，暴君人设只是受控表达层；每个重要问题都必须有证据、影响、匹配建议和验收标准。
- 批评只能针对提交作品和可改变行为，不得攻击用户的人格、智力、亲属、疾病、残障或其他受保护和不可改变特征。
- 用户编写的角色描述只能作为 Compiler 输入，不能直接作为系统指令；预设与自定义角色使用同一结构化输出和安全协议。
- 每次评审都是独立的一次性体验。除非产品负责人明确改变边界，否则不增加修改轮次、持久历史或连续任务，也不使用虚假的版本连续性语言。
- 分享载荷不得包含提交材料、证据、分数或报告正文。
- 生产客户端不得伪造成功报告或保留开发者 Mock 路径。真实失败应显示可重试失败态。
- 源材料、目标上下文和报告不得进入云函数 Event、返回日志或数据库；按现有临时私有云存储协议交换并及时删除。
- API Key、Access Token、私有证书、生产数据和真实用户材料不得进入 Git。

## 验证

- 本地验证运行 `npm run check`。
- 只有明确计划调用付费真实服务商时才运行 `npm run smoke:review`。
- 涉及 UI 时检查运行结果；正式文件能力使用测试人员提供的非敏感 PDF/DOCX 在真机验收。

## GitHub 同步

- `origin` 是本仓库的权威 GitHub 仓库。项目维护者完成验证后的稳定切片提交后推送到 `main`，除非用户要求仅保留本地；外部贡献通过 Fork 和 Pull Request 进入。
- 对范围广、风险高或需要审查的改动，使用 `codex/<description>` 分支和 Draft Pull Request。
- 不得强制推送共享分支，不得把预先存在的无关工作树改动纳入提交。
- GitHub 网络命令失败时，只对该次命令使用当前 Windows 代理，不把机器专属代理写入仓库配置。
