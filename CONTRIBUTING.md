# 参与贡献

感谢你愿意让“别骂了”变得更专业、更可靠。这个项目欢迎小而完整、能够验证的贡献。

## 适合贡献的内容

- 可复现的小程序、云函数或文件解析缺陷。
- 使用虚构或充分脱敏材料编写的角色评测案例。
- 能提高证据准确性、修改可执行性或安全边界的角色引擎改进。
- 对部署文档、无障碍、错误恢复和开发体验的聚焦优化。

涉及产品范围、新数据留存、第三方服务、付费能力或表达边界的改动，请先开 Issue 说明使用场景和取舍，再开始大规模实现。

## 本地开发

需要 Node.js 20.19 或更高版本。

```bash
npm ci
npm run check
```

`npm run check` 不调用真实 AI 服务。只有在你已配置自己的云环境、理解调用成本并明确需要端到端验证时，才运行 `npm run check:provider` 或 `npm run smoke:review`。

## 提交要求

1. 一个 PR 解决一个清晰问题，避免混入无关重构或生成文件。
2. 行为变化应带最窄有效测试；修改 AI 输出协议或角色量表时，同时更新相关评测案例。
3. 不通过跳过测试、削弱断言或隐藏失败来让检查通过。
4. README、API 或部署行为发生变化时，同步更新对应文档。
5. PR 描述说明动机、改动、验证证据与已知限制。

允许使用 AI 辅助开发，但提交者必须理解最终代码、检查许可与隐私风险，并对验证结果负责。

## 数据与素材红线

- 不提交真实用户材料、聊天记录、简历、报告或未经授权的截图。
- 不提交 AppID、CloudBase 环境 ID、SecretId、SecretKey、Token、证书或本地 `.env`。
- 测试材料必须是虚构内容或已经无法回溯到个人与组织的充分脱敏内容。
- 新增第三方或模型生成素材时，在 PR 中注明来源、授权和限制。
- 安全问题不要发公开 Issue，请按 [安全政策](SECURITY.md) 私下报告。

## 评审原则

维护者会优先确认：改动是否有真实用户价值、是否保持证据链与一次性体验、是否引入新的隐私或滥用风险，以及验证是否覆盖实际改动面。

参与讨论时请批评方案和作品，不攻击贡献者的人格、身份或能力。这也是产品自身坚持的边界；完整约定见 [社区行为准则](CODE_OF_CONDUCT.md)。
